# Train/Validation/Test Splits

This folder stores dataset split artifacts, such as lists of file paths or generated split metadata.

The actual split files are created during the data pipeline stage after the raw MRI dataset is available.
