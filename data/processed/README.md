# Processed Dataset Folder

This folder is reserved for processed image arrays, metadata, or derived dataset artifacts generated by preprocessing scripts.

The actual contents will be created after the dataset is placed in `data/raw/` and preprocessing is run.
